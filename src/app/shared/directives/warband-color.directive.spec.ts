import { WarbandColorDirective } from './warband-color.directive';

describe('WarbandColorDirective', () => {
  it('should create an instance', () => {
    const directive = new WarbandColorDirective();
    expect(directive).toBeTruthy();
  });
});
